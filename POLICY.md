# POLICY.md

## C++ 구현 규칙

### 디버깅/버그 수정 로그 워크플로우

- 유저가 디버깅이나 버그 수정을 요청하면, 바로 추정 수정에 들어가기 전에 의심되는 실행 경로와 상태 전환 지점에 임시 추적 로그를 붙인다.
- 로그는 런타임에서 수집하고, 재현 시점의 로그 순서와 상태값을 분석한 뒤 원인을 좁힌다.
- 로그 메시지는 `ActionTrace`, `CameraRotationTrace`처럼 필터링 가능한 고유 프리픽스를 포함하고, 프레임 번호, 액터/컴포넌트 이름, 핵심 상태값을 함께 남긴다.
- 원인 분석과 수정이 끝나면 유저가 유지하길 원하는 로그가 아닌 임시 로그는 제거하거나 별도 커밋으로 분리한다.

### Unreal Engine Unity Build 호환성

- Unreal Engine unity build에서는 여러 `.cpp` 파일이 하나의 번역 단위로 묶일 수 있으므로, 익명 네임스페이스나 `static`만으로 파일 간 이름 충돌을 피할 수 있다고 가정하지 않는다.
- `.cpp` 로컬 헬퍼 함수, 상수, 타입은 `DebugBoolText`, `ResolveEightWayDirection`, `NormalizeInput`처럼 범용적인 이름을 단독으로 쓰지 않는다.
- 파일 로컬 헬퍼 이름은 파일이나 도메인을 포함해 고유하게 작성한다. 예: `DodgeDebugBoolText`, `ResolveDodgeEightWayDirection`, `ActionDebugBoolText`.
- 새 로컬 헬퍼를 추가하기 전에 모듈 전체에서 같은 이름이 이미 쓰이는지 검색한다.
- 여러 파일에서 같은 로직이 필요하면 같은 구현을 복사하지 말고 명시적인 공용 유틸리티로 분리해 하나의 정의를 공유한다.
- unity build 충돌을 피하려고 빌드 설정을 우회하기보다 소스 코드가 unity build에서도 안전하게 컴파일되도록 고친다.

## 커밋 메시지 컨벤션

커밋 메시지는 Conventional Commits 계열의 타입을 기준으로 하되, 실제 커밋 제목과 설명은 한글로 작성한다.

### 기본 형식

```text
<한글 타입>: <변경 요약> (#이슈번호)

* <한글 타입>: <상세 변경 내용>
* <한글 타입>: <추가 변경 내용>
```

이슈 번호가 없으면 `(#이슈번호)`는 생략한다.

### 타입 표기

| Conventional 타입 | 한글 타입 |
| --- | --- |
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 리팩터링 |
| `perf` | 성능 개선 |
| `docs` | 문서 수정 |
| `test` | 테스트 수정 |
| `build` | 빌드 수정 |
| `ci` | CI 수정 |
| `chore` | 작업 정리 |

### 작성 규칙

- 제목은 한 줄로 작성하고, 변경 내용을 짧고 명확하게 요약한다.
- 제목과 본문은 모두 한글로 작성한다.
- 본문은 변경 사항을 `*` 목록으로 작성한다.
- 본문 항목도 제목과 같은 한글 타입으로 시작한다.
- 여러 변경이 포함된 경우 가장 중요한 변경 타입을 제목에 사용한다.

### 예시

```text
버그 수정: 타이틀 메뉴 마우스 입력 복구 (#144)

* 버그 수정: 타이틀 메뉴에서 마우스 입력이 동작하지 않던 문제 복구
* 버그 수정: 로딩 화면 자동 종료 적용
```

```text
기능 추가: 캐릭터 선택 화면 기본 입력 처리 추가

* 기능 추가: 캐릭터 선택 화면에서 키보드와 마우스 입력 처리 추가
* 기능 추가: 선택한 캐릭터 정보를 게임 시작 단계로 전달
```

```text
리팩터링: 로딩 화면 상태 전환 로직 정리

* 리팩터링: 로딩 완료 판정 로직을 별도 함수로 분리
* 리팩터링: 중복된 화면 전환 호출 제거
```

## 문서와 지식 그래프 운영

- 문서의 역할과 갱신 시점, Graphify 명령, pre-push 절차는 `docs/wiki/Documentation-Workflow.md`를 단일 운영 기준으로 삼는다.
- `docs/wiki/`는 사람이 Obsidian으로 읽고 직접 갱신하는 내부 위키다. Graphify 생성물이 이 위키를 대신하지 않는다.
- `docs/todo/`는 진행 중인 작업의 단기 상태와 검증 기록만 보관한다. push 전 위키 검토 결과를 `갱신: <문서>` 또는 `변경 불필요: <이유>`로 기록하고, 완료하면 작업 파일을 삭제한다. 장기 구조, 설계 의도, 팀 공통 지식은 `docs/wiki/`에 반영한다.
- PR 설명에도 TODO의 위키 검토 결과를 옮겨 리뷰어가 문서 영향을 확인할 수 있게 한다.
- 주요 C++ 타입의 책임·라이프사이클·불변조건은 선언과 가장 가까운 헤더의 Doxygen 형식 문서 블록에 유지한다. 필수 대상, 품질 기준, 생략 조건은 `docs/wiki/Header-Documentation.md`를 따른다.
- `/** ... */` 존재 여부 자체를 목표로 삼지 않는다. 빈 블록, Unreal 템플릿 주석, 미래 계획만 적은 블록은 문서화로 인정하지 않는다.
- `graphify-out/`은 에이전트의 코드 탐색을 위한 생성 지식창고다. 그래프, 리포트, 에이전트용 생성 위키는 직접 수정하지 않고 Graphify로 재생성한다.
- 각 커밋 뒤 공식 Graphify 훅의 AST 증분 갱신을 안전망으로 사용한다. 이 훅은 문서 의미 추출과 생성 뷰를 갱신하지 않으므로 중간 산출물만 독립적으로 커밋하지 않는다.
- 초기화된 일반 작업 트리에서 `git switch`나 `git checkout <branch|commit>`으로 브랜치 또는 커밋을 전환하면 공식 `post-checkout` 훅이 새 snapshot의 전체 코드 corpus를 백그라운드에서 다시 추출한다. 즉시 graph를 질의할 때는 현재 전환의 재구축 완료를 먼저 확인한다. 경로 단위 checkout·`git restore`·`git reset`, merge·rebase·cherry-pick 중, `graphify-out/`이 없는 브랜치와 linked worktree에서는 이 훅을 갱신 보장으로 간주하지 않는다.
- 원격 push 전에는 사람용 위키 검토, 위키 문서의 의미 추출, 에이전트용 wiki·HTML 재생성, wrap-up stamp 커밋을 완료하고 저장소의 pre-push 최신성 검사를 통과해야 한다.
